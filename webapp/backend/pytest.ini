[pytest]
# The real suite lives in tests/. Scope collection there so a stray scratch
# script left at the backend root that happens to match test_*.py isn't
# collected as a bogus test (its functions get treated as test cases and
# error on missing "fixtures").
testpaths = tests
